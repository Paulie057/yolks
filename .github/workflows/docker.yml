name: Build & Push Docker Image

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Log in to GHCR
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin

      - name: Set lowercase owner
        run: echo "OWNER_LC=${GITHUB_REPOSITORY_OWNER,,}" >> $GITHUB_ENV

      - name: Debug owner
        run: echo "Owner is $OWNER_LC"

      - name: Build image
        run: docker build -t ghcr.io/${{ env.OWNER_LC }}/yolks:rs2vietnam-wine:latest .

      - name: Push image
        run: docker push ghcr.io/${{ env.OWNER_LC }}/yolks:rs2vietnam-wine:latest
